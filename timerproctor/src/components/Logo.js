import styled from 'styled-components'
const LogoContainer = styled.div`
  user-select: none;
  font-weight: bold;
  font-size: 24px;
  color: black;
`

const Logo = () => <LogoContainer>TimerProctor</LogoContainer>

export default Logo
